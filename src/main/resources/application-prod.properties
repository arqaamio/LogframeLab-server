server.port=8080
# MySQL datasource
spring.datasource.url=jdbc:mysql://${MYSQL_DB_HOST}/${MYSQL_DB_NAME}?useSSL=false
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${MYSQL_DB_USER}
spring.datasource.password=${MYSQL_DB_PASSWORD}
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.database-platform=org.hibernate.dialect.MySQL57Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.generate-ddl=true
# spring.jpa.show-sql=true

#spring.flyway.enabled=false
#spring.flyway.sql-migration-prefix=V
#spring.flyway.repeatable-sql-migration-prefix=R
#spring.flyway.sql-migration-separator=__
#spring.flyway.sql-migration-suffixes=.sql

#spring.flyway.url=jdbc:mysql://${MYSQL_DB_HOST}/${MYSQL_DB_NAME}?useSSL=false
#spring.flyway.schemas=${MYSQL_DB_NAME}
#spring.flyway.user=${MYSQL_DB_USER}
#spring.flyway.password=${MYSQL_DB_PASSWORD}
spring.flyway.locations=classpath:prod/db/migration
